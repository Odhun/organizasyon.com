rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read images (public CDN)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
